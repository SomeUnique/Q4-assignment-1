"# Q4-assignment-1" 
Propmt : You are given an image attachment. Use this image to generate the complete HTML and CSS code.Task:Generate a fully structured, clean, and responsive "index.html" and "style.css" that visually match the design of the attached image from A to Z. Replicate:- Layout- Colors- Gradients- Fonts- Font sizes- Buttons- Navigation bar- Spacing & alignment- Background and UI elementsRequirements:
1. HTML and CSS should be in two separate files.
2. Code must match the design exactly as shown in the image.
3. Do not assume other elements—only recreate what is visible.
4. Use Google Fonts if needed.
5. Use modern CSS (flexbox /gradient backgrounds).Here is the image for reference:<paste your img path here >